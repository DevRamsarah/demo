export interface UserData {
    emp_id: string;
    emp_name: string;
    date_submit: string;
    batch_id: string;
    //claim_date: string;
    // despatch_date: string;
    batch_date_to: string;
    claim_id: string;
}